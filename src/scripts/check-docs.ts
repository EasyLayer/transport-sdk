#!/usr/bin/env ts-node
import chalk from 'chalk';

console.log(
  chalk.blue(
    "Don't forget to update the documentation file DOCS.md if you made changes related to Transport API or installation!"
  )
);
